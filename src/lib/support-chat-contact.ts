import type { ChatThreadContact } from "@/types/live-chat";

/** Khung đại diện nhân viên / bot hỗ trợ phía khách hàng. */
export const DEFAULT_SUPPORT_CONTACT: ChatThreadContact = {
    id: "support",
    name: "Hỗ trợ Botforge",
    avatar: "https://i.pravatar.cc/128?img=68",
};
