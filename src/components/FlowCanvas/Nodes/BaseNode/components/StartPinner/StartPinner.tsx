import { Play } from "lucide-react";

function StartPinner() {
    return (
        <div className="absolute -top-4 left-0 flex -translate-y-full items-center gap-2 bg-white">
            <Play /> Điểm bắt đầu
        </div>
    );
}

export default StartPinner;
