"use client";

import BaseNode from "@/components/FlowCanvas/Nodes/BaseNode/BaseNode";

function CollectionNode(props: any) {
    return <BaseNode childProps={props}>Collection Node</BaseNode>;
}

export default CollectionNode;
