/**
 * Live chat HTTP API — dùng `liveChatApi` (REST + axios).
 * File này re-export để tương thích import cũ `chatService`.
 */
export { liveChatApi as chatService, liveChatApi } from "./liveChatApi";
